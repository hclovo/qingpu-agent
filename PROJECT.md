# 氢擎 Agent — 氢能产业商机与精准获客 Agent

> 一句话介绍：把「行业信号 → 商机评分 → 产品匹配 → 跟进建议」全自动串起来的氢能销售工作台；Agent 提议，人来定，让少量销售人员也能高效跟进大量分散线索。

- 适用企业：北京氢璞创能科技有限公司（聚焦燃料电池电堆/系统与 PEM 制氢装备）
- 文档版本：V1.0 · 2026-08-20
- 面向读者：黑客松评委、业务方、技术评审

---

## 1. 项目背景与真实问题

### 1.1 行业背景

氢能产业正处于商业化爬坡阶段：燃料电池重卡、矿卡、船舶、轨道交通、无人机、PEM 制氢、园区储能等场景百花齐放，但「信号散、决策重、对手快」。一个商机从招标公告、政策文件、伙伴会议、产业新闻里浮现后，往往需要销售在数天内判断：

- 它属不属于氢璞覆盖的场景？
- 现在跟、还是先放着？
- 该派哪条产品线对接？
- 谁去谈、谈什么、风险在哪？

### 1.2 真实痛点（来自氢璞创能业务访谈）

氢璞创能市场推广人员极少，但同时需要覆盖 **客户、潜客、上游厂商、生态伙伴** 四类关系。当前真实痛点有四：

| 痛点 | 业务表现 | 后果 |
| --- | --- | --- |
| 线索多、人员少 | 销售一个人盯几十条商机 | 高潜商机被淹没，错过跟进窗口 |
| 评估口径不一致 | 「该不该跟」靠经验和感觉 | 决策不可复核，新人接手成本高 |
| 跟进建议缺乏依据 | 沟通材料靠临时拼凑 | 对外口径不一致，技术承诺有风险 |
| 关系容易「沉默」 | 客户、伙伴、供应商久未联系 | 关系温度下降，机会流失 |

### 1.3 我们要解决的核心问题

> 让一个销售人员也能在 **3 分钟内** 从看板进入某条商机、看清「为什么值得跟、该用什么产品、风险在哪、下一步是什么」，并且每一条结论都能追到来源。

---

## 2. 目标用户与应用场景

### 2.1 一类用户：氢能企业的市场/销售/供应链人员

- 场景 A：早上 9 点打开工作台，看「今日该联系谁 / 为什么联系 / 该准备什么」。
- 场景 B：在客户来访前，用自然语言问「这家客户最近的需求和我们 OCEAN200 的契合点」，得到带引用和切入点的回答。
- 场景 C：粘贴一段公开项目公告 / 政策 / 新闻，得到 0-100 分的优先级评分、产品匹配候选和风险点，再决定是否立项跟进。
- 场景 D：在与上游厂商、伙伴沟通前，快速看到「上次联系在什么时候、下一步该约什么」。

### 2.2 二类用户：评审、合作伙伴、客户管理层

- 在明亮会议室投影演示 Agent 能力，10 分钟内看清业务闭环。
- 设计原则是「两米外可读、字号 12px 起、状态不靠颜色区分」，专门为演示场景优化。

---

## 3. 解决方案

### 3.1 产品定位

不是传统 CRM，而是「**企业关系与商机自动化 Agent 的可视化工作台**」：

- **Agent 是入口**：自然语言对话统一查询关系、知识、产品和商机。
- **关系 + 知识 + 商机是三类长期上下文**：Agent 在这之间穿梭，而不是孤立回答。
- **Web 是数据入口和确认界面**：人工补充知识、确认 Agent 建议、更新阶段。
- **硬边界**：报价、产品选型、技术承诺、对外联系，必须由人确认。Agent 不自动外发。

### 3.2 工作流闭环

```text
行业信号发现 → 潜客识别 → 商机评分 → 产品匹配 → Agent 研判 → 人工确认 → 跟进
       ↑                                                    ↓
       └─────────────── 知识库与关系上下文持续补充 ──────────┘
```

---

## 4. 核心功能

| 模块 | 关键能力 |
| --- | --- |
| **F01 Agent 对话与每日简报** | 自然语言查询企业 / 关系 / 商机；每日简报：到期跟进、沉默关系、高潜商机、知识缺口 |
| **F02 关系中心** | 客户 / 潜客 / 上游厂商 / 伙伴 四类；健康度、最近联系、下一步行动、互动时间线 |
| **F03 知识库与数据补充** | 文本 / URL / 文件 三种来源；处理状态 + 来源标签；立即可被 Agent 检索 |
| **F04 业务总览** | 商机数、高潜数、本周新增、平均分；等级 / 行业分布；高潜排行 |
| **F05 商机雷达与筛选** | 关键词 / 行业 / 等级 / 阶段 多维筛选；Agent 自动发现入口 |
| **F06 商机详情与证据链** | 客户画像、信号来源、评分六维拆解、产品 Top 3 匹配、风险点、阶段管理 |
| **F07 新信号研判** | 表单输入一段信号 → 确定性评分 + 产品匹配 + Agent 解释 → 落库为商机 |
| **F08 Agent 自动发现** | 联网/演示两条路径：联网用 Tavily + LLM 结构化抽取；无密钥时返回明确标注的演示结果 |
| **F09 线索阶段管理** | 新线索 → 待核验 → 已研判 → 跟进中 → 已转化 → 已关闭 |
| **F10 产品知识基线** | 电堆 / 车用系统 / 船用系统 / 制氢装备 四大产品家族；功率、寿命、认证、场景 |

### 4.1 评分规则 V1.0（确定性、可解释）

总分 100 分，代表「跟进优先级」，**不**代表成交概率：

| 维度 | 权重 | 关注点 |
| --- | ---: | --- |
| 场景/产品匹配 | 30 | 是否属于氢璞覆盖场景；功率 / 认证 / 家族是否匹配 |
| 需求与采购能力 | 20 | 数量、预算、客户规模、重复采购或示范项目能力 |
| 信号时效 | 20 | 30 / 90 / 180 / 365 天衰减分档 |
| 项目成熟度 | 15 | 关注 → 规划 → 立项 → 招标 → 试运营 → 复购 |
| 可触达性 | 10 | 公开商务渠道 / 既有伙伴 / 明确项目主体 |
| 战略价值 | 5 | 标杆客户、重点区域、可复制场景、产业链协同 |

等级：A 80-100（高潜）/ B 65-79（重点培育）/ C 50-64（持续观察）/ D 0-49（低优先级）。

### 4.2 降级模式（无密钥也能跑）

系统设计「三层降级」，任何时候都能完整演示：

1. **智能模式**：配置 `OPENAI_API_KEY` 等，Mastra Agent 启用工具调用与结构化输出。
2. **规则模式**：未配置密钥时，意图路由 + 关键词检索 + 确定性评分仍能完整回答。
3. **演示发现模式**：无 Tavily 实时搜索密钥时，返回明确标注的演示候选。

界面顶栏始终展示当前模式，**规则模式不是错误状态**，用户始终能分辨数据来源。

---

## 5. 产品 / Demo 展示

### 5.1 演示路径（10 分钟）

```text
① Agent 工作台               →  输入「今天该联系谁 / 高潜商机清单」
② 业务总览（Dashboard）      →  看核心指标、等级分布、高潜排行
③ 商机雷达（Opportunities）  →  按 A 级筛选，点开某条 B 级商机
④ 商机详情（Detail）          →  看评分六维雷达图、产品 Top 3、证据链、Agent 建议
⑤ 关系中心（Relationships）   →  切到对应客户，补充一条互动记录
⑥ 知识库（Knowledge）         →  新增一段文本/URL 知识
⑦ 信号研判（Analyze）        →  粘贴一段新信号，得分 + 匹配 + 研判一键入库
```

### 5.2 主要界面（已实现页面）

| 页面 | 文件 | 关键交互 |
| --- | --- | --- |
| Agent 工作台 | `apps/web/src/pages/workspace.tsx` | 对话、简报、推荐问句、来源引用 |
| 业务总览 | `apps/web/src/pages/dashboard.tsx` | 指标、等级分布、行业分布、动态活动 |
| 关系中心 | `apps/web/src/pages/relationships.tsx` | 角色 Tab、抽屉式详情、互动记录、待办 |
| 知识库 | `apps/web/src/pages/knowledge.tsx` | 检索、新增文本/URL/文件、来源标签 |
| 商机雷达 | `apps/web/src/pages/opportunities.tsx` | 多维筛选、自动发现抽屉、阶段切换 |
| 信号研判 | `apps/web/src/pages/analyze.tsx` | 结构化表单 → 评分 → 产品 → 落库 |
| 商机详情 | `apps/web/src/pages/opportunity-detail.tsx` | 雷达图、产品 Top 3、证据链、阶段更新 |
| 产品知识 | `apps/web/src/pages/products.tsx` | 家族 Tab、参数边界、来源出处 |

### 5.3 视觉与交互设计（DESIGN.md）

设计名：「**研判台 / The Analyst's Desk**」。专门为明亮会议室投影优化：

- 亮色主表面 + 深色焦点侧栏，**两米外可读**。
- 双语义色：**深潜青 = 可推进**，**锻铜 = 需人工确认**，**警示赤 = 系统错误**（三色不可互换）。
- 12px 字号地板，所有数字 `tabular-nums` 同列对齐。
- 1px 边框 + 三级明度分层代替阴影（投影下阴影不可见）。
- 状态必须同时用文字 + 形状 + 颜色三重表达，照顾色盲与投影褪色。
- 拒绝环保绿刷满、渐变按钮、叶子图标；也拒绝低对比密表和「一键完成」的危险动作样式。

---

## 6. 技术实现方式

### 6.1 整体架构

```text
┌──────────────────────────────────────────────────────────────┐
│  React + Vite + React Router      浏览器端，纯前端，无密钥     │
└──────────────────────────────────────────────────────────────┘
                          │ /api (同源代理 / VITE_API_BASE_URL)
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  Hono (Node.js 22.13+)        API Routes + Zod 校验 + CORS    │
└──────────────────────────────────────────────────────────────┘
       │                              │              │
       ▼                              ▼              ▼
┌──────────────┐         ┌────────────────────┐   ┌────────────┐
│ Domain 层     │         │ Mastra Runtime     │   │ Business   │
│ 纯函数评分    │         │ 三个 Agent + Tools │   │ Service    │
│ + 产品匹配    │         │ 智能/降级三态       │   │ 编排       │
└──────────────┘         └────────────────────┘   └────────────┘
                                  │
                                  ▼
                       ┌─────────────────────┐
                       │ OpenAI / 兼容服务    │
                       │ (MASTRA_MODEL +     │
                       │  OPENAI_API_KEY)    │
                       └─────────────────────┘

数据层：BusinessStore 接口 → PostgreSQL/Drizzle（生产）/ 内存（演示）
```

### 6.2 技术栈

| 层级 | 选型 | 理由 |
| --- | --- | --- |
| 工作区 | pnpm workspace + TypeScript 5 | 统一依赖类型 |
| Web | React 18 + Vite + React Router + Lucide | 快速构建响应式工作台 |
| API | Hono + Node.js 22.13+ | 轻量、标准 Fetch、易测 |
| Agent | `@mastra/core` + 自建 Tools | 官方 TypeScript Agent 能力，支持结构化输出与工具调用 |
| 契约 | Zod 4 | 前后端共享类型与运行时校验 |
| 数据 | Drizzle ORM + PostgreSQL（可降级内存） | 一份异步接口两种实现 |
| 搜索 | Tavily（联网发现） + 垂直信号源 | 联网候选 + 招投标/政策站点提示 |
| 测试 | Vitest | 单元 + API + Tool 测试 |

### 6.3 Monorepo 结构

```text
apps/
  api/                 Hono API + Mastra Agents/Tools + Drizzle
  web/                 React + Vite Agent 工作台
packages/
  contracts/           前后端共享 Zod 契约
  domain/              纯函数评分 + 产品匹配（不依赖模型）
docs/                  需求 / 设计 / 数据库 / 部署 / RBAC 文档
T04_…/                 企业原始资料（只读）
```

### 6.4 三个 Agent 与 Tools

```text
氢擎 Agent 工作台
├── Enterprise Relationship Agent   用户主入口
│   ├── searchKnowledgeTool         检索企业知识库
│   ├── listRelationshipsTool       读取四类关系
│   ├── listOpportunitiesTool       读取商机
│   └── matchProductsTool           按场景匹配产品
├── Opportunity Analysis Agent      把确定性评分翻译成销售语言
│   ├── scoreOpportunityTool        V1.0.0 纯函数评分
│   └── matchProductsTool           硬约束 + 软匹配 Top 3
└── Opportunity Research Agent      联网/垂直源发现候选信号
    ├── webSearchTool               Tavily 实时检索
    └── verticalSignalSearchTool    招投标/政策站点
```

关键约束：**确定性评分不可被 Agent 改写**，Agent 只负责解释、切入、风险、建议。

### 6.5 演示种子数据

为了本地一键可跑，系统内置：

- **7 条产品**（ST 系列电堆、E200 车用、OCEAN 船用、CESP 制氢、空冷电堆）
- **多条关系**（客户 / 潜客 / 上游 / 伙伴，明示模拟）
- **7 条典型商机**（覆盖船舶、重卡、港口、矿区、产业园、轨交、无人机场景）
- **企业知识条目**（来源标注「公开 / 脱敏 / 模拟」三态）
- 全部商机名称带「【模拟】」前缀，UI 上有 demo 标签，避免被误读为真实数据。

### 6.6 安全与降级

- 模型密钥只在后端 `.env` 读取，**不会进入前端产物或 API 响应**。
- Web 与 API 跨域白名单：`WEB_ORIGIN` / `WEB_ORIGINS`，禁止 `*`。
- Agent 输入做防提示注入：网页文本只作为事实材料，不能改写系统约束。
- 三层降级：智能 → 规则 → 演示；任意一层失败都安全回退，UI 明示当前模式。
- 用户与权限（RBAC）已落地：`admin` / `sales` / `support` 三类角色 + 9 个权限点，会话 Cookie 鉴权。
- Stage 变更、Agent 调用都返回 `requestId`，便于追溯。

---

## 7. 项目创新点

1. **「Agent 提议 + 人工确认」硬边界** — 不是把 Agent 当万能助手，而是把 Agent 限制在「分析、解释、建议」环节；所有报价、技术承诺、对外联系必须由人确认。这条边界在 UI 上用专用色 + 提示语直接画出来，避免被误读为「自动化一切」。
2. **确定性评分 + Agent 解释双层架构** — 评分是 100% 可复现的纯函数（V1.0.0 版本号固定），Agent 只能解释不能改写。这让「业务规则可审计、模型升级不回溯历史分」。
3. **三层降级可演示** — 没有 OpenAI 密钥、没有 Tavily 密钥，系统仍能完整跑通「对话 / 评分 / 匹配 / 发现 / 阶段管理」全流程；且每种降级都在 UI 顶部明示，**规则模式不是错误状态**。
4. **数据来源可追溯** — 每条证据都带「来源类型 + 标题 + URL + 发生时间 + 抓取时间 + 摘录 + 置信度」七要素；模拟数据全部带 demo 标签，绝不与公开事实混淆。
5. **演示场景优先的视觉系统** — 不是「漂亮」，而是「在投影上两米外还能看清」。拒绝阴影承担结构职责、拒绝环保绿刷满、拒绝把低对比密表当工作台。
6. **关系 × 知识 × 商机三轴上下文** — 不做单点工具。Agent 在「这家客户最近有没有相关的政策信号」「我们 OCEAN200 能不能对接」之间穿梭，而不是孤立地回答。
7. **0 文档里的 0 个「待补充」** — 整套系统 monorepo 一键启动，本地不配密钥也能完整演示，p99 列表 API < 300ms。

---

## 8. 实际应用与商业价值

### 8.1 实际应用（已落到代码层面）

| 维度 | 现状 |
| --- | --- |
| 产品形态 | 前后端分离 Web 应用，已在 Vercel 上成功部署（见 `docs/Vercel前后端分离部署指南.md`） |
| 数据存储 | PostgreSQL + Drizzle，生产可用；未配置时自动降级内存，开箱即可演示 |
| 鉴权 | Cookie 会话 + RBAC（admin / sales / support）+ 9 个权限点 |
| 测试 | Vitest 覆盖 domain、agent、knowledge、relationship、API、store |
| 一键启动 | `pnpm install && pnpm dev` 同时拉起 Hono API (4111) 和 Vite Web (5173) |

### 8.2 商业价值

**对氢能企业：**

- **人效提升**：少量销售人员即可覆盖大量分散商机，把「该跟谁」从经验判断变成 0-100 可解释分。
- **风险下降**：每条对外建议都附依据和「待核实」标记，报价与技术承诺必经人工确认。
- **知识沉淀**：客户互动、产业信号、产品匹配资料长期可检索，不再因人员流动丢失。
- **进入门槛低**：未配置大模型密钥也能完整运行；后续引入密钥无需重构。

**对生态伙伴：**

- 可作为「氢能企业销售工作台参考实现」输出给上下游厂商与同行。
- 评分规则 V1.0.0、产品匹配矩阵、术语卡都可作为行业知识资产对外提供。

**可扩展方向**（已留接口）：

- OCR + 人工校验台，把 PDF 产品手册入库为带页码的结构化知识。
- PostgreSQL 上叠加 pgvector / `@mastra/pg`，混合结构化过滤 + RAG 检索。
- 接入招投标、政策、产业园区、企业新闻等合规数据源。
- 与企业 CRM 双向同步，用真实输赢数据校准评分权重（V1 权重只是可解释业务规则）。
- 引入人工反馈、Agent Eval、Prompt 版本和数据质量看板。

---

## 9. 快速验证（评委 5 分钟可跑）

```bash
git clone <repo>
cd qingpu-agent
cp .env.example .env         # 不配密钥也能跑
pnpm install
pnpm dev                     # 同时启动 API 4111 + Web 5173
```

打开 <http://localhost:5173>：

1. 进 Agent 工作台，输入「今天该联系谁」 → 看到规则模式的简报 + 建议行动。
2. 进业务总览 → 看指标、等级分布、高潜排行。
3. 进商机雷达 → 选 A 级，看「长江清能航运」详情 → 看评分雷达图 + 产品匹配 + 证据链 + Agent 研判。
4. 进信号研判 → 粘贴一段新信号 → 一键得到评分 + 匹配 + 落库。
5. 健康检查 <http://localhost:4111/api/health> 会返回 `{ "agentMode": "rules" | "intelligent", "storage": "memory" | "postgres" }`。

配置 `OPENAI_API_KEY` 后重启，Agent 顶栏的 `rules` 标记会自动切换为 `intelligent`，联网商机发现会从演示数据切换到 Tavily 实时检索。

---

## 10. 一句话总结

> **氢擎 Agent 把氢能销售从「凭感觉挑客户」变成「按证据排商机」**：Agent 负责发现、解释、建议，人负责确认、跟进、承诺；无密钥能跑、配密钥更强，演示与生产共用一套代码。